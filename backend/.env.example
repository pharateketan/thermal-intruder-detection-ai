# Copy this to .env and edit as needed

MODEL_PATH=models/thermal_intruder_model.pt
DEVICE=cpu          # use "0" or "cuda" for GPU
DEFAULT_CONF=0.45
DEFAULT_IOU=0.50
DEFAULT_MAX_DET=100
IMG_SIZE=640
HOST=0.0.0.0
PORT=8000
RELOAD=true
